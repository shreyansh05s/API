# label-studio/Dockerfile
FROM heartexlabs/label-studio:latest

# Expose Label Studio on port 8080
EXPOSE 8080

# Run Label Studio
CMD ["label-studio", "start", "--port", "8080", "--host", "0.0.0.0"]
